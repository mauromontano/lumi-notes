# Lumi Notes v1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Agregar 5 features a Lumi Notes: acciones en notificaciones de recordatorio, tags automáticos por IA con filtro, compartir texto hacia la app, App Shortcut de Siri para dictar, y notas cifradas en Keychain con Face ID.

**Architecture:** Fases 1-2 son puro JS/TS sobre el build actual (expo-notifications categories; columna `tag` + prompt del formatter + chips de filtro). Fases 3-5 agregan capacidades nativas vía config plugins (expo-share-intent, App Intent Swift local, expo-secure-store con Face ID) y requieren `npx expo prebuild` + rebuild del dev client. La DB pasa a migraciones versionadas con `PRAGMA user_version`.

**Tech Stack:** Expo SDK 57 / RN 0.86, expo-router, expo-sqlite, expo-notifications, expo-secure-store, expo-share-intent, Swift App Intents, Jest + better-sqlite3 (tests de DB en memoria).

**Spec:** `docs/superpowers/specs/2026-07-04-v1.1-features-design.md`

## Global Constraints

- Antes de correr `jest`/`eslint`/`tsc`/`ctx7`: `export PATH="$HOME/.nvm/versions/node/v22.22.0/bin:$PATH"` (Node 22 requerido).
- AGENTS.md: leer docs versionadas de Expo v57 (https://docs.expo.dev/versions/v57.0.0/) antes de escribir código que use APIs de Expo.
- Copy de UI en español rioplatense (voseo), consistente con la app existente.
- Logs con el util existente `src/lib/log.ts` (prefijo `[lumi]`, solo `__DEV__`). No usar `console.*` directo.
- Set fijo de tags: `compras`, `trabajo`, `ideas`, `personal`, `salud`, `viajes`. Un tag por nota o `null`.
- Las notas cifradas (`secure = 1`) NUNCA pasan por la IA (ni formatNote, ni editNote, ni tags automáticos).
- Límite de nota cifrada: 2000 bytes UTF-8. Límite de texto compartido: 8000 caracteres.
- Identificadores de notificación: categoría `reminder`, acciones `snooze-1h` y `complete`.
- Commit por tarea con mensajes `feat:`/`test:`/`chore:` + `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Verificación estándar de cada tarea JS: `npx jest <archivo> && npx tsc --noEmit`. El lint del repo tiene 1 error preexistente conocido en `useDictation.ts:41` (react-hooks/immutability) — no introducir nuevos.

---

## FASE 1 — Acciones en notificaciones

### Task 1: Lógica pura de acciones (`planReminderAction`)

**Files:**
- Create: `src/reminders/actions.ts`
- Test: `src/reminders/__tests__/actions.test.ts`

**Interfaces:**
- Consumes: `Recurrence` de `@/notes/types`.
- Produces: `REMINDER_CATEGORY: 'reminder'`, `ACTION_SNOOZE_1H: 'snooze-1h'`, `ACTION_COMPLETE: 'complete'`, `type ReminderActionPlan = { kind: 'snooze'; snoozeUntil: Date } | { kind: 'clear-reminder' } | { kind: 'ignore' } | { kind: 'open-note' }`, `planReminderAction(actionIdentifier: string, recurrence: Recurrence, now: Date): ReminderActionPlan`.

- [ ] **Step 1: Escribir el test que falla**

```ts
// src/reminders/__tests__/actions.test.ts
import { planReminderAction, ACTION_SNOOZE_1H, ACTION_COMPLETE } from '../actions';

describe('planReminderAction', () => {
  const now = new Date('2026-07-04T10:00:00.000Z');

  it('snooze agenda 1 hora después, sin importar la recurrencia', () => {
    const plan = planReminderAction(ACTION_SNOOZE_1H, 'none', now);
    expect(plan).toEqual({ kind: 'snooze', snoozeUntil: new Date('2026-07-04T11:00:00.000Z') });
    expect(planReminderAction(ACTION_SNOOZE_1H, 'daily', now).kind).toBe('snooze');
  });

  it('completar un recordatorio puntual limpia el recordatorio', () => {
    expect(planReminderAction(ACTION_COMPLETE, 'none', now)).toEqual({ kind: 'clear-reminder' });
  });

  it('completar un recurrente solo descarta la ocurrencia (ignore)', () => {
    expect(planReminderAction(ACTION_COMPLETE, 'daily', now)).toEqual({ kind: 'ignore' });
    expect(planReminderAction(ACTION_COMPLETE, 'weekly', now)).toEqual({ kind: 'ignore' });
    expect(planReminderAction(ACTION_COMPLETE, 'monthly', now)).toEqual({ kind: 'ignore' });
  });

  it('el tap default abre la nota', () => {
    expect(planReminderAction('expo.modules.notifications.actions.DEFAULT', 'none', now)).toEqual({ kind: 'open-note' });
  });
});
```

- [ ] **Step 2: Correr el test y verificar que falla**

Run: `npx jest src/reminders/__tests__/actions.test.ts`
Expected: FAIL — `Cannot find module '../actions'`.

- [ ] **Step 3: Implementación mínima**

```ts
// src/reminders/actions.ts
import type { Recurrence } from '@/notes/types';

export const REMINDER_CATEGORY = 'reminder';
export const ACTION_SNOOZE_1H = 'snooze-1h';
export const ACTION_COMPLETE = 'complete';

export type ReminderActionPlan =
  | { kind: 'snooze'; snoozeUntil: Date }
  | { kind: 'clear-reminder' }
  | { kind: 'ignore' }
  | { kind: 'open-note' };

const HOUR_MS = 60 * 60 * 1000;

export function planReminderAction(
  actionIdentifier: string,
  recurrence: Recurrence,
  now: Date,
): ReminderActionPlan {
  if (actionIdentifier === ACTION_SNOOZE_1H) {
    return { kind: 'snooze', snoozeUntil: new Date(now.getTime() + HOUR_MS) };
  }
  if (actionIdentifier === ACTION_COMPLETE) {
    return recurrence === 'none' ? { kind: 'clear-reminder' } : { kind: 'ignore' };
  }
  return { kind: 'open-note' };
}
```

- [ ] **Step 4: Verificar que pasa**

Run: `npx jest src/reminders/__tests__/actions.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/reminders/actions.ts src/reminders/__tests__/actions.test.ts
git commit -m "feat: lógica pura de acciones de recordatorio (posponer/completar)"
```

### Task 2: Ejecutor de acciones + categoría de notificación

**Files:**
- Modify: `src/reminders/actions.ts` (agregar `applyReminderAction`)
- Modify: `src/reminders/scheduler.ts` (categoría + `registerReminderCategory`)
- Test: `src/reminders/__tests__/actions.test.ts` (ampliar)

**Interfaces:**
- Consumes: `getNote`, `updateNote` de `@/db/notesRepo`; `scheduleReminder`, `cancelReminder` de `./scheduler`; `DbLike` de `@/db/types`; `createTestDb` de `tests/helpers/testDb`.
- Produces: `applyReminderAction(db: DbLike, noteId: string, plan: ReminderActionPlan, deps?: { schedule?: (note: {id: string; title: string}, at: Date, rec: Recurrence) => Promise<string>; cancel?: (id: string | null) => Promise<void> }): Promise<void>` y `registerReminderCategory(): Promise<void>` en scheduler.

- [ ] **Step 1: Ampliar el test (falla)**

Agregar a `src/reminders/__tests__/actions.test.ts`:

```ts
import { applyReminderAction } from '../actions';
import { createTestDb } from '../../../tests/helpers/testDb';
import { createNote, getNote, updateNote } from '../../db/notesRepo';

describe('applyReminderAction', () => {
  const now = new Date('2026-07-04T10:00:00.000Z');
  const snoozeUntil = new Date('2026-07-04T11:00:00.000Z');

  async function setup(recurrence: 'none' | 'daily') {
    const db = createTestDb();
    await createNote(db, { title: 'Regar plantas', body: '' }, { id: 'n1', now });
    await updateNote(db, 'n1', {
      reminderAt: now.toISOString(), reminderRecurrence: recurrence, notificationId: 'old-notif',
    }, now);
    return db;
  }

  it('snooze en puntual: reagenda y actualiza reminderAt/notificationId', async () => {
    const db = await setup('none');
    const schedule = jest.fn().mockResolvedValue('new-notif');
    const cancel = jest.fn().mockResolvedValue(undefined);
    await applyReminderAction(db, 'n1', { kind: 'snooze', snoozeUntil }, { schedule, cancel });
    expect(schedule).toHaveBeenCalledWith({ id: 'n1', title: 'Regar plantas' }, snoozeUntil, 'none');
    const n = await getNote(db, 'n1');
    expect(n?.notificationId).toBe('new-notif');
    expect(n?.reminderAt).toBe(snoozeUntil.toISOString());
  });

  it('snooze en recurrente: agenda extra sin tocar la nota', async () => {
    const db = await setup('daily');
    const schedule = jest.fn().mockResolvedValue('new-notif');
    await applyReminderAction(db, 'n1', { kind: 'snooze', snoozeUntil }, { schedule });
    expect(schedule).toHaveBeenCalled();
    const n = await getNote(db, 'n1');
    expect(n?.notificationId).toBe('old-notif'); // el id recurrente se preserva
    expect(n?.reminderRecurrence).toBe('daily');
  });

  it('clear-reminder cancela y limpia la nota', async () => {
    const db = await setup('none');
    const cancel = jest.fn().mockResolvedValue(undefined);
    await applyReminderAction(db, 'n1', { kind: 'clear-reminder' }, { cancel });
    expect(cancel).toHaveBeenCalledWith('old-notif');
    const n = await getNote(db, 'n1');
    expect(n?.reminderAt).toBeNull();
    expect(n?.reminderRecurrence).toBe('none');
    expect(n?.notificationId).toBeNull();
  });

  it('ignore y open-note no tocan la DB; nota inexistente no explota', async () => {
    const db = await setup('none');
    await applyReminderAction(db, 'n1', { kind: 'ignore' });
    await applyReminderAction(db, 'nope', { kind: 'clear-reminder' });
    expect((await getNote(db, 'n1'))?.notificationId).toBe('old-notif');
  });
});
```

- [ ] **Step 2: Correr y verificar que falla**

Run: `npx jest src/reminders/__tests__/actions.test.ts`
Expected: FAIL — `applyReminderAction is not a function`.

Nota: `actions.test.ts` ahora importa (indirectamente vía deps default) `scheduler.ts` que importa `expo-notifications`. Si Jest falla resolviendo `expo-notifications`, usar import dinámico de deps default dentro de la función (ver Step 3, que evita el import estático circular usando parámetros opcionales y `require` diferido NO es necesario: importamos scheduler estáticamente y el mock de Jest existente para expo-notifications — revisar `jest.config`/mocks existentes; si no hay mock, agregar a la config `moduleNameMapper` un stub `tests/mocks/expo-notifications.ts` que exporte funciones vacías).

- [ ] **Step 3: Implementar**

Agregar a `src/reminders/actions.ts`:

```ts
import type { DbLike } from '@/db/types';
import { getNote, updateNote } from '@/db/notesRepo';
import { scheduleReminder, cancelReminder } from './scheduler';
import { log } from '@/lib/log';

interface ActionDeps {
  schedule?: typeof scheduleReminder;
  cancel?: typeof cancelReminder;
}

export async function applyReminderAction(
  db: DbLike,
  noteId: string,
  plan: ReminderActionPlan,
  deps: ActionDeps = {},
): Promise<void> {
  if (plan.kind === 'ignore' || plan.kind === 'open-note') return;
  const schedule = deps.schedule ?? scheduleReminder;
  const cancel = deps.cancel ?? cancelReminder;
  const note = await getNote(db, noteId);
  if (!note) return;
  try {
    if (plan.kind === 'snooze') {
      const newId = await schedule({ id: note.id, title: note.title }, plan.snoozeUntil, 'none');
      if (note.reminderRecurrence === 'none') {
        await updateNote(db, note.id, { reminderAt: plan.snoozeUntil.toISOString(), notificationId: newId });
      }
      // recurrente: la notificación extra se consume sola; el schedule recurrente sigue intacto
    } else {
      await cancel(note.notificationId);
      await updateNote(db, note.id, { reminderAt: null, reminderRecurrence: 'none', notificationId: null });
    }
  } catch (e) {
    log.warn('acción de recordatorio falló:', plan.kind, (e as Error).message);
  }
}
```

En `src/reminders/scheduler.ts`: importar `REMINDER_CATEGORY`, `ACTION_SNOOZE_1H`, `ACTION_COMPLETE` desde `./actions` y:

```ts
export async function registerReminderCategory(): Promise<void> {
  await Notifications.setNotificationCategoryAsync(REMINDER_CATEGORY, [
    { identifier: ACTION_SNOOZE_1H, buttonTitle: 'Posponer 1h', options: { opensAppToForeground: false } },
    { identifier: ACTION_COMPLETE, buttonTitle: 'Completar', options: { opensAppToForeground: false } },
  ]);
}
```

Y en `scheduleReminder`, el content pasa a:

```ts
    content: { title: 'Lumi ✦', body: note.title, sound: true, data: { noteId: note.id }, categoryIdentifier: REMINDER_CATEGORY },
```

(Verificar nombres exactos de la API en https://docs.expo.dev/versions/v57.0.0/sdk/notifications/ — `setNotificationCategoryAsync` y `categoryIdentifier`.)

- [ ] **Step 4: Verificar que pasa**

Run: `npx jest src/reminders && npx tsc --noEmit`
Expected: PASS todos los tests de reminders; tsc limpio.

- [ ] **Step 5: Commit**

```bash
git add src/reminders tests
git commit -m "feat: ejecutor de acciones de recordatorio y categoría de notificación"
```

### Task 3: Wiring en la app (`useReminderResponses`)

**Files:**
- Create: `src/reminders/useReminderResponses.ts`
- Modify: `src/app/_layout.tsx` (reemplaza el listener actual)

**Interfaces:**
- Consumes: `planReminderAction`, `applyReminderAction`, `REMINDER_CATEGORY` (Task 1-2), `registerReminderCategory` (Task 2), `getDb` de `@/db/database`, `getNote` de `@/db/notesRepo`.
- Produces: hook `useReminderResponses(): void` que registra la categoría, maneja respuestas (listener + última respuesta al arrancar) y rutea `open-note` a `/note/<id>`.

- [ ] **Step 1: Implementar el hook**

```ts
// src/reminders/useReminderResponses.ts
import { useEffect } from 'react';
import * as Notifications from 'expo-notifications';
import { router } from 'expo-router';
import { getDb } from '@/db/database';
import { getNote } from '@/db/notesRepo';
import { planReminderAction, applyReminderAction } from './actions';
import { registerReminderCategory } from './scheduler';
import { log } from '@/lib/log';

const handled = new Set<string>(); // sobrevive re-mounts del layout

async function handleResponse(response: Notifications.NotificationResponse): Promise<void> {
  const key = `${response.notification.request.identifier}:${response.actionIdentifier}:${response.notification.date}`;
  if (handled.has(key)) return;
  handled.add(key);
  const noteId = response.notification.request.content.data?.noteId;
  if (typeof noteId !== 'string') return;
  const db = getDb();
  const note = await getNote(db, noteId);
  if (!note) return;
  const plan = planReminderAction(response.actionIdentifier, note.reminderRecurrence, new Date());
  log.debug('respuesta de notificación:', response.actionIdentifier, '→', plan.kind);
  if (plan.kind === 'open-note') {
    router.push(`/note/${noteId}`);
    return;
  }
  await applyReminderAction(db, noteId, plan);
}

export function useReminderResponses(): void {
  useEffect(() => {
    void registerReminderCategory();
    // respuesta recibida con la app cerrada
    void Notifications.getLastNotificationResponseAsync().then((r) => { if (r) void handleResponse(r); });
    const sub = Notifications.addNotificationResponseReceivedListener((r) => { void handleResponse(r); });
    return () => sub.remove();
  }, []);
}
```

- [ ] **Step 2: Reemplazar el listener en `_layout.tsx`**

En `src/app/_layout.tsx`, dentro de `Screens()`, borrar el `useEffect` actual (líneas 18-24) y reemplazar por:

```ts
  useReminderResponses();
```

con el import `import { useReminderResponses } from '../reminders/useReminderResponses';`. Quitar los imports que queden sin uso (`useEffect`, `router`) si ya no se usan en el archivo.

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit && npx jest && npx eslint src --max-warnings=10`
Expected: tsc limpio, tests PASS, sin errores nuevos de lint.

Manual (iPhone, build actual): crear nota con recordatorio a +2 min → al llegar la notificación, mantener presionada → deben aparecer "Posponer 1h" y "Completar". Probar Completar (el recordatorio desaparece de la nota) y Posponer (badge de la nota pasa a +1h). Probar también con la app cerrada.

- [ ] **Step 4: Commit**

```bash
git add src/reminders/useReminderResponses.ts src/app/_layout.tsx
git commit -m "feat: acciones posponer/completar en notificaciones de recordatorio"
```

---

## FASE 2 — Tags automáticos por IA + filtro

### Task 4: Set de tags y colores

**Files:**
- Create: `src/notes/tags.ts`
- Test: `src/notes/__tests__/tags.test.ts`

**Interfaces:**
- Consumes: `ThemeName` de `@/theme/colors`.
- Produces: `NOTE_TAGS` (readonly array), `type NoteTag`, `isNoteTag(x: unknown): x is NoteTag`, `tagColors: Record<ThemeName, Record<NoteTag, { bg: string; text: string }>>`.

- [ ] **Step 1: Test que falla**

```ts
// src/notes/__tests__/tags.test.ts
import { NOTE_TAGS, isNoteTag, tagColors } from '../tags';

describe('tags', () => {
  it('valida solo tags del set', () => {
    expect(isNoteTag('compras')).toBe(true);
    expect(isNoteTag('viajes')).toBe(true);
    expect(isNoteTag('random')).toBe(false);
    expect(isNoteTag(null)).toBe(false);
    expect(isNoteTag(42)).toBe(false);
  });
  it('hay colores para cada tag en ambos temas', () => {
    for (const t of NOTE_TAGS) {
      expect(tagColors.dark[t].bg).toBeTruthy();
      expect(tagColors.light[t].text).toBeTruthy();
    }
  });
});
```

- [ ] **Step 2: Correr y ver que falla** — `npx jest src/notes/__tests__/tags.test.ts` → FAIL (módulo inexistente).

- [ ] **Step 3: Implementar**

```ts
// src/notes/tags.ts
import type { ThemeName } from '@/theme/colors';

export const NOTE_TAGS = ['compras', 'trabajo', 'ideas', 'personal', 'salud', 'viajes'] as const;
export type NoteTag = (typeof NOTE_TAGS)[number];

export function isNoteTag(x: unknown): x is NoteTag {
  return typeof x === 'string' && (NOTE_TAGS as readonly string[]).includes(x);
}

export const tagColors: Record<ThemeName, Record<NoteTag, { bg: string; text: string }>> = {
  dark: {
    compras:  { bg: 'rgba(107,232,168,0.18)', text: '#8ef0bd' },
    trabajo:  { bg: 'rgba(124,107,255,0.20)', text: '#b3a7ff' },
    ideas:    { bg: 'rgba(155,232,255,0.18)', text: '#9be8ff' },
    personal: { bg: 'rgba(231,139,181,0.20)', text: '#f2a9cc' },
    salud:    { bg: 'rgba(255,122,122,0.18)', text: '#ff9d9d' },
    viajes:   { bg: 'rgba(255,184,138,0.20)', text: '#ffc9a3' },
  },
  light: {
    compras:  { bg: '#e0f7e9', text: '#2f9668' },
    trabajo:  { bg: '#e8e4ff', text: '#6b5bd6' },
    ideas:    { bg: '#e0f3fa', text: '#2a7f9e' },
    personal: { bg: '#ffe4ef', text: '#c05585' },
    salud:    { bg: '#ffe3e3', text: '#c0392b' },
    viajes:   { bg: '#ffe8d6', text: '#c97b3d' },
  },
};
```

- [ ] **Step 4: Verificar** — `npx jest src/notes/__tests__/tags.test.ts` → PASS.

- [ ] **Step 5: Commit** — `git add src/notes && git commit -m "feat: set fijo de tags con colores por tema"`

### Task 5: Migraciones versionadas + columna `tag` en repo

**Files:**
- Modify: `src/db/schema.ts` (pasa a `MIGRATIONS: string[]`)
- Modify: `src/db/database.ts` (aplica migraciones con `PRAGMA user_version`)
- Modify: `tests/helpers/testDb.ts` (aplica `MIGRATIONS`)
- Modify: `src/db/notesRepo.ts` + `src/notes/types.ts`
- Test: `src/db/__tests__/notesRepo.test.ts` (ampliar)

**Interfaces:**
- Produces: `MIGRATIONS: string[]` en schema (índice+1 = versión); `Note.tag: string | null`; `createNote(db, { title, body, tag? })`; `updateNote` acepta `tag` en el patch; `listNotes(db, search?, tag?)`.

- [ ] **Step 1: Tests que fallan**

Agregar a `src/db/__tests__/notesRepo.test.ts`:

```ts
  it('guarda y filtra por tag', async () => {
    const d = db();
    await createNote(d, { title: 'Pan', body: '', tag: 'compras' }, { id: 'a' });
    await createNote(d, { title: 'Idea app', body: '', tag: 'ideas' }, { id: 'b' });
    await createNote(d, { title: 'Sin tag', body: '' }, { id: 'c' });
    expect((await getNote(d, 'a'))?.tag).toBe('compras');
    expect((await getNote(d, 'c'))?.tag).toBeNull();
    expect((await listNotes(d, undefined, 'compras')).map((n) => n.id)).toEqual(['a']);
    expect((await listNotes(d)).length).toBe(3);
  });

  it('combina búsqueda y tag, y actualiza el tag', async () => {
    const d = db();
    await createNote(d, { title: 'Pan lactal', body: '', tag: 'compras' }, { id: 'a' });
    await createNote(d, { title: 'Pan de campo', body: '', tag: 'ideas' }, { id: 'b' });
    expect((await listNotes(d, 'pan', 'compras')).map((n) => n.id)).toEqual(['a']);
    await updateNote(d, 'b', { tag: null });
    expect((await getNote(d, 'b'))?.tag).toBeNull();
  });
```

Run: `npx jest src/db` → FAIL (columna/params inexistentes).

- [ ] **Step 2: Migraciones**

`src/db/schema.ts` completo:

```ts
// Migraciones versionadas: índice + 1 = PRAGMA user_version resultante.
// La v1 usa IF NOT EXISTS así los installs previos (user_version 0) pasan sin romper.
export const MIGRATIONS: string[] = [
  `
CREATE TABLE IF NOT EXISTS notes (
  id TEXT PRIMARY KEY,
  title TEXT NOT NULL,
  body TEXT NOT NULL,
  pinned INTEGER NOT NULL DEFAULT 0,
  reminder_at TEXT,
  reminder_recurrence TEXT NOT NULL DEFAULT 'none',
  notification_id TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_notes_order ON notes (pinned DESC, updated_at DESC);
`,
  `ALTER TABLE notes ADD COLUMN tag TEXT;`,
];
```

`src/db/database.ts` — reemplazar el bloque de apertura:

```ts
import * as SQLite from 'expo-sqlite';
import * as Crypto from 'expo-crypto';
import { MIGRATIONS } from './schema';
import type { DbLike } from './types';

let instance: SQLite.SQLiteDatabase | null = null;

function migrate(db: SQLite.SQLiteDatabase): void {
  const row = db.getFirstSync<{ user_version: number }>('PRAGMA user_version');
  for (let v = row?.user_version ?? 0; v < MIGRATIONS.length; v++) {
    db.execSync(MIGRATIONS[v]);
    db.execSync(`PRAGMA user_version = ${v + 1}`);
  }
}

export function getDb(): DbLike {
  if (!instance) {
    instance = SQLite.openDatabaseSync('lumi-notes.db');
    migrate(instance);
  }
  return instance as unknown as DbLike;
}

export function newId(): string {
  return Crypto.randomUUID();
}
```

`tests/helpers/testDb.ts` — reemplazar `db.exec(MIGRATION_SQL)` por:

```ts
import { MIGRATIONS } from '../../src/db/schema';
// dentro de createTestDb():
for (const sql of MIGRATIONS) db.exec(sql);
```

(quitar el import de `MIGRATION_SQL`; buscar otros usos de `MIGRATION_SQL` con grep y actualizarlos).

- [ ] **Step 3: Repo y tipos**

`src/notes/types.ts` — agregar a `Note`: `tag: string | null;` (después de `notificationId`).

`src/db/notesRepo.ts`:
- `Row`: agregar `tag: string | null;`
- `toNote`: agregar `tag: r.tag,`
- `createNote`: input pasa a `{ title: string; body: string; tag?: string | null }`, INSERT:

```ts
  await db.runAsync(
    `INSERT INTO notes (id, title, body, pinned, reminder_at, reminder_recurrence, notification_id, tag, created_at, updated_at)
     VALUES (?, ?, ?, 0, NULL, 'none', NULL, ?, ?, ?)`,
    [id, input.title, input.body, input.tag ?? null, ts, ts],
  );
```

- `updateNote`: el `Pick` suma `'tag'`; el UPDATE agrega `tag=?` con `next.tag` en el orden correspondiente.
- `listNotes(db, search?, tag?)`: construir WHERE dinámico:

```ts
export async function listNotes(db: DbLike, search?: string, tag?: string): Promise<Note[]> {
  const where: string[] = [];
  const params: unknown[] = [];
  if (search && search.trim()) {
    const q = `%${search.trim().toLowerCase()}%`;
    where.push('(lower(title) LIKE ? OR lower(body) LIKE ?)');
    params.push(q, q);
  }
  if (tag) { where.push('tag = ?'); params.push(tag); }
  const sql = `SELECT * FROM notes${where.length ? ` WHERE ${where.join(' AND ')}` : ''} ORDER BY pinned DESC, updated_at DESC`;
  const rows = await db.getAllAsync<Row>(sql, params);
  return rows.map(toNote);
}
```

- [ ] **Step 4: Verificar** — `npx jest && npx tsc --noEmit` → PASS/limpio (los tests viejos de listNotes siguen andando).

- [ ] **Step 5: Commit** — `git add src/db src/notes tests && git commit -m "feat: migraciones versionadas y columna tag en notas"`

### Task 6: El formatter devuelve tag

**Files:**
- Modify: `src/ai/formatter.ts` (tipo + parseo)
- Modify: `src/ai/claudeFormatter.ts` (prompt)
- Test: `src/ai/__tests__/formatter.test.ts` (ampliar)

**Interfaces:**
- Consumes: `isNoteTag`, `NoteTag` de `@/notes/tags`.
- Produces: `FormattedNote { title: string; body: string; tag: NoteTag | null }` — `parseFormatterResponse` SIEMPRE devuelve `tag` (null si falta o es inválido).

- [ ] **Step 1: Tests que fallan**

Agregar a `src/ai/__tests__/formatter.test.ts`:

```ts
  it('parsea el tag cuando es válido', () => {
    const r = parseFormatterResponse('{"titulo":"Súper","cuerpo":"- pan","tag":"compras"}');
    expect(r.tag).toBe('compras');
  });
  it('tag inválido o ausente ⇒ null (no rompe)', () => {
    expect(parseFormatterResponse('{"titulo":"x","cuerpo":"y","tag":"inventado"}').tag).toBeNull();
    expect(parseFormatterResponse('{"titulo":"x","cuerpo":"y"}').tag).toBeNull();
  });
```

Run: `npx jest src/ai` → FAIL.

- [ ] **Step 2: Implementar**

`src/ai/formatter.ts`:

```ts
import { isNoteTag, type NoteTag } from '@/notes/tags';

export interface FormattedNote { title: string; body: string; tag: NoteTag | null }
```

y al final de `parseFormatterResponse`:

```ts
  const tag = isNoteTag(rec['tag']) ? rec['tag'] : null;
  return { title: titulo.trim(), body: cuerpo, tag };
```

`src/ai/claudeFormatter.ts` — `SYSTEM_PROMPT` pasa a:

```ts
const SYSTEM_PROMPT = `Sos Lumi, el asistente de una app de notas. Convertís dictados en notas prolijas en español.
Respondé SOLO con JSON válido, sin texto extra, con esta forma exacta:
{"titulo": "título corto y claro", "cuerpo": "texto de la nota", "tag": "una categoría o null"}
Reglas:
- Si el dictado enumera cosas, el cuerpo usa bullets markdown: "- item" (uno por línea).
- Corregí puntuación y muletillas, pero NO inventes contenido que no se dictó.
- El título resume la nota en pocas palabras.
- "tag" debe ser exactamente una de: "compras", "trabajo", "ideas", "personal", "salud", "viajes". Si ninguna aplica bien, usá null.`;
```

Arreglar los fallbacks de `voice.tsx` que construyen `FormattedNote` a mano (`{ title: 'Nota dictada', body: transcript }` y el draft inicial `{ title: '', body: '' }`) agregando `tag: null`, y en `editNote` de `claudeFormatter.ts` incluir el tag actual en el JSON de "Nota actual" para que la IA pueda mantenerlo o cambiarlo:

```ts
        `Nota actual:\n{"titulo": ${JSON.stringify(current.title)}, "cuerpo": ${JSON.stringify(current.body)}, "tag": ${JSON.stringify(current.tag)}}\n\nInstrucción del usuario: """${instruction}"""\n\nDevolvé la nota modificada aplicando SOLO lo que pide la instrucción.`,
```

En `voice.tsx`, donde se llama `formatter.editNote({ title: original!.title, body: original!.body }, ...)` pasar también `tag: isNoteTag(original!.tag) ? original!.tag : null`.

- [ ] **Step 3: Verificar** — `npx jest && npx tsc --noEmit` → PASS/limpio (tsc va a marcar todos los constructores de FormattedNote sin tag: arreglarlos agregando `tag: null`).

- [ ] **Step 4: Commit** — `git add src/ai src/app && git commit -m "feat: el formatter clasifica la nota con un tag del set fijo"`

### Task 7: UI de tags (chips de filtro, badge, selector, guardado)

**Files:**
- Create: `src/components/TagChips.tsx`
- Modify: `src/app/index.tsx` (filtro), `src/components/NoteCard.tsx` (badge), `src/app/note/[id].tsx` (selector manual), `src/app/voice.tsx` (guardar tag del draft)

**Interfaces:**
- Consumes: `NOTE_TAGS`, `NoteTag`, `isNoteTag`, `tagColors` (Task 4); `listNotes(db, search, tag)` (Task 5); `draft.tag` (Task 6).
- Produces: `TagChips({ selected, onSelect, includeNone? })` componente reusable.

- [ ] **Step 1: Componente `TagChips`**

```tsx
// src/components/TagChips.tsx
import React from 'react';
import { ScrollView, Pressable, Text, StyleSheet } from 'react-native';
import { useTheme } from '../theme/ThemeContext';
import { NOTE_TAGS, tagColors, type NoteTag } from '../notes/tags';

type Props = {
  selected: NoteTag | null;
  onSelect: (tag: NoteTag | null) => void;
  includeNone?: boolean; // chip "sin tag" para el editor
};

export function TagChips({ selected, onSelect, includeNone }: Props) {
  const { theme, palette } = useTheme();
  return (
    <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>
      {includeNone ? (
        <Pressable
          onPress={() => onSelect(null)}
          style={[styles.chip, {
            borderColor: palette.cardBorder,
            backgroundColor: selected === null ? palette.card : 'transparent',
          }]}
        >
          <Text style={{ color: palette.textMuted, fontSize: 13 }}>sin tag</Text>
        </Pressable>
      ) : null}
      {NOTE_TAGS.map((t) => {
        const c = tagColors[theme][t];
        const active = selected === t;
        return (
          <Pressable
            key={t}
            onPress={() => onSelect(active ? null : t)}
            style={[styles.chip, {
              backgroundColor: active ? c.bg : 'transparent',
              borderColor: active ? c.text : palette.cardBorder,
            }]}
          >
            <Text style={{ color: active ? c.text : palette.textMuted, fontSize: 13 }}>{t}</Text>
          </Pressable>
        );
      })}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  row: { gap: 8, paddingHorizontal: 16, paddingVertical: 4 },
  chip: { borderWidth: 1, borderRadius: 999, paddingHorizontal: 12, paddingVertical: 6 },
});
```

- [ ] **Step 2: Filtro en home (`src/app/index.tsx`)**

- Estado nuevo: `const [tagFilter, setTagFilter] = useState<NoteTag | null>(null);`
- `refresh` pasa a `setNotes(await listNotes(getDb(), q || undefined, t || undefined));` con firma `(q: string, t: NoteTag | null)`; el `useFocusEffect` llama `refresh(search, tagFilter)` y depende de ambos.
- Debajo de `<SearchBar …/>`: `<TagChips selected={tagFilter} onSelect={setTagFilter} />`.
- Imports: `TagChips`, `type NoteTag` de `../notes/tags`.

- [ ] **Step 3: Badge en `NoteCard.tsx`**

- `const { theme, palette } = useTheme();` y después del badge de recordatorio:

```tsx
      {note.tag && isNoteTag(note.tag) ? (
        <View style={[styles.badge, { backgroundColor: tagColors[theme][note.tag].bg }]}>
          <Text style={[styles.badgeText, { color: tagColors[theme][note.tag].text }]}>{note.tag}</Text>
        </View>
      ) : null}
```

con imports `isNoteTag, tagColors` de `../notes/tags`. (Los dos badges pueden convivir; envolverlos en un `<View style={{ flexDirection: 'row', gap: 6 }}>` si quedan apilados feos.)

- [ ] **Step 4: Selector manual en `note/[id].tsx` y guardado**

- Estado: `const [tag, setTag] = useState<NoteTag | null>(null);` — al cargar la nota, `setTag(isNoteTag(n.tag) ? n.tag : null);`
- UI entre el body y el `ReminderPicker`: `<TagChips selected={tag} onSelect={setTag} includeNone />`
- `save()`: incluir `tag` en `createNote(db, { title: finalTitle, body, tag })` y en `updateNote(db, note!.id, { title: finalTitle, body, pinned, tag })`.

- [ ] **Step 5: Guardar el tag del draft en `voice.tsx`**

- `save()`: `createNote(db, { title: draft.title, body: draft.body, tag: draft.tag })` y en edición `updateNote(db, original!.id, { title: draft.title, body: draft.body, tag: draft.tag })`.
- En el preview, mostrar el tag elegible: `<TagChips selected={draft.tag} onSelect={(t) => setDraft((d) => ({ ...d, tag: t }))} includeNone />` debajo del TextInput del cuerpo.

- [ ] **Step 6: Verificar**

Run: `npx tsc --noEmit && npx jest && npx eslint src --max-warnings=10`
Manual (iPhone): dictar "comprar pan y leche" (con key) → tag `compras` sugerido en preview; filtrar por chip en home; editar tag a mano en la nota; nota sin tag no muestra badge.

- [ ] **Step 7: Commit** — `git add src && git commit -m "feat: chips de tags: filtro en home, badge y selector manual"`

---

## FASE 3 — Compartir texto hacia Lumi

### Task 8: Normalización del texto compartido

**Files:**
- Create: `src/notes/sharedText.ts`
- Test: `src/notes/__tests__/sharedText.test.ts`

**Interfaces:**
- Produces: `SHARED_TEXT_LIMIT = 8000`, `prepareSharedText(raw: string): string` (trim + colapsa saltos triples + trunca con "…").

- [ ] **Step 1: Test que falla**

```ts
// src/notes/__tests__/sharedText.test.ts
import { prepareSharedText, SHARED_TEXT_LIMIT } from '../sharedText';

describe('prepareSharedText', () => {
  it('recorta espacios y colapsa saltos múltiples', () => {
    expect(prepareSharedText('  hola\n\n\n\nmundo  ')).toBe('hola\n\nmundo');
  });
  it('trunca a 8000 chars con elipsis', () => {
    const long = 'a'.repeat(SHARED_TEXT_LIMIT + 500);
    const out = prepareSharedText(long);
    expect(out.length).toBe(SHARED_TEXT_LIMIT);
    expect(out.endsWith('…')).toBe(true);
  });
  it('texto corto queda igual', () => {
    expect(prepareSharedText('hola')).toBe('hola');
  });
});
```

Run: `npx jest src/notes/__tests__/sharedText.test.ts` → FAIL.

- [ ] **Step 2: Implementar**

```ts
// src/notes/sharedText.ts
export const SHARED_TEXT_LIMIT = 8000;

export function prepareSharedText(raw: string): string {
  const clean = raw.trim().replace(/\n{3,}/g, '\n\n');
  if (clean.length <= SHARED_TEXT_LIMIT) return clean;
  return clean.slice(0, SHARED_TEXT_LIMIT - 1) + '…';
}
```

- [ ] **Step 3: Verificar** — `npx jest src/notes` → PASS.
- [ ] **Step 4: Commit** — `git add src/notes && git commit -m "feat: normalización de texto compartido"`

### Task 9: expo-share-intent + flujo sharedText en voice

**Files:**
- Modify: `package.json` (dep nueva), `app.json` (plugin), `src/app/_layout.tsx` (hook), `src/app/voice.tsx` (param `sharedText` + refactor `processTranscript`)

**Interfaces:**
- Consumes: `prepareSharedText` (Task 8); `useShareIntent` de `expo-share-intent`.
- Produces: ruta `/voice?sharedText=<texto>` que saltea el dictado y va directo a thinking → preview.

- [ ] **Step 1: Instalar y configurar**

```bash
npm install expo-share-intent
```

IMPORTANTE: la tabla de compatibilidad del README (https://github.com/achorein/expo-share-intent) llega a SDK 56 con v7. Verificar ahí la versión para **SDK 57** e instalarla explícitamente (`npm install expo-share-intent@<major-para-sdk57>`). Si aún no existe soporte SDK 57, frenar esta tarea y avisar al usuario (no degradar el SDK).

En `app.json`, agregar a `plugins`: `"expo-share-intent"` (defaults = solo texto y URLs en iOS, que es lo que queremos).

- [ ] **Step 2: Refactor en `voice.tsx` — extraer `processTranscript`**

Extraer de `finishDictation` todo lo posterior a la validación de transcript vacío a:

```ts
  async function processTranscript(transcript: string) {
    setHint(null);
    setPhase('thinking');
    setOrbState('thinking');
    try {
      const formatted = isEdit
        ? await formatter.editNote(
            { title: original!.title, body: original!.body, tag: isNoteTag(original!.tag) ? original!.tag : null },
            transcript,
          )
        : await formatter.formatNote(transcript);
      setDraft(formatted);
      setUsedRawFallback(null);
      setOrbState('success');
    } catch (e) {
      const kind = e instanceof FormatterError ? e.kind : 'api';
      log.warn('formatter falló:', kind, '-', (e as Error).message);
      if (isEdit) {
        setDraft({ title: original!.title, body: original!.body, tag: isNoteTag(original!.tag) ? original!.tag : null });
        setUsedRawFallback(
          kind === 'no-key'
            ? 'Sin API key configurada: no puedo editar por voz. Configurala en Ajustes.'
            : 'Lumi no pudo aplicar la edición (sin conexión o error). La nota queda como estaba.',
        );
      } else {
        setDraft({ title: 'Nota dictada', body: transcript, tag: null });
        setUsedRawFallback(
          kind === 'no-key'
            ? 'Sin API key configurada: guardo la transcripción sin formatear.'
            : 'Lumi no pudo formatear (sin conexión o error). Guardo la transcripción cruda.',
        );
      }
      setOrbState('error');
    }
    setPhase('preview');
  }
```

`finishDictation` queda: validar edición, `dictation.stop()`, chequear transcript vacío (mismo hint actual), y `await processTranscript(transcript)`.

- [ ] **Step 3: Param `sharedText` en `voice.tsx`**

```ts
  const { noteId, sharedText } = useLocalSearchParams<{ noteId?: string; sharedText?: string }>();
  const isShare = typeof sharedText === 'string' && sharedText.length > 0;
```

El `useEffect` de mount pasa a:

```ts
  useEffect(() => {
    if (isShare) {
      void processTranscript(prepareSharedText(sharedText!));
      return;
    }
    (async () => {
      const result = await dictation.start();
      if (result === 'denied') setPermissionDenied(true);
    })();
    // solo al montar
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);
```

En "Re-dictar" del preview: si `isShare`, ocultar el botón (no hay dictado que repetir). Título de nota compartida sin key: cambiar el fallback `'Nota dictada'` por `isShare ? 'Nota compartida' : 'Nota dictada'`.

- [ ] **Step 4: Hook de share en `_layout.tsx`**

En `Screens()`:

```ts
  const { hasShareIntent, shareIntent, resetShareIntent } = useShareIntent();
  useEffect(() => {
    if (!hasShareIntent) return;
    const text = shareIntent.text ?? shareIntent.webUrl;
    if (text) router.push({ pathname: '/voice', params: { sharedText: text } });
    resetShareIntent();
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [hasShareIntent]);
```

con `import { useShareIntent } from 'expo-share-intent';` y `router` de expo-router.

- [ ] **Step 5: Rebuild y verificar**

```bash
npx expo prebuild -p ios
# build del dev client (según flujo habitual del repo, p.ej.:)
npx expo run:ios --device
```

`npx tsc --noEmit && npx jest` → limpio/PASS.
Manual: compartir un párrafo desde Safari/Notas → Lumi abre en thinking → preview con nota formateada (o cruda sin key) → guardar. Verificar que el share con la app ya abierta también funciona.

- [ ] **Step 6: Commit** — `git add package.json package-lock.json app.json src && git commit -m "feat: compartir texto hacia Lumi con preview IA (expo-share-intent)"`

---

## FASE 4 — App Shortcut de Siri

### Task 10: App Intent Swift + config plugin local

**Files:**
- Create: `ios-assets/DictarNotaIntent.swift`
- Create: `plugins/withAppIntents.js`
- Modify: `app.json` (agregar `"./plugins/withAppIntents.js"` a plugins)

**Interfaces:**
- Produces: shortcut de Siri "Dictar nota en Lumi" que abre `luminotes://voice`.

- [ ] **Step 1: El intent en Swift**

```swift
// ios-assets/DictarNotaIntent.swift
import AppIntents
import UIKit

struct DictarNotaIntent: AppIntent {
  static var title: LocalizedStringResource = "Dictar nota"
  static var description = IntentDescription("Abre Lumi listo para dictar una nota por voz.")
  static var openAppWhenRun: Bool = true

  @MainActor
  func perform() async throws -> some IntentResult {
    if let url = URL(string: "luminotes://voice") {
      await UIApplication.shared.open(url)
    }
    return .result()
  }
}

struct LumiShortcuts: AppShortcutsProvider {
  static var appShortcuts: [AppShortcut] {
    AppShortcut(
      intent: DictarNotaIntent(),
      phrases: [
        "Dictar nota en \(.applicationName)",
        "Nueva nota de voz en \(.applicationName)",
        "New voice note in \(.applicationName)",
      ],
      shortTitle: "Dictar nota",
      systemImageName: "mic.circle.fill"
    )
  }
}
```

- [ ] **Step 2: Config plugin local**

```js
// plugins/withAppIntents.js
const { withXcodeProject, IOSConfig } = require('@expo/config-plugins');
const fs = require('fs');
const path = require('path');

// Copia el App Intent al target principal en cada prebuild (sobrevive --clean).
module.exports = function withAppIntents(config) {
  return withXcodeProject(config, (cfg) => {
    const projectName = cfg.modRequest.projectName;
    const src = path.join(cfg.modRequest.projectRoot, 'ios-assets', 'DictarNotaIntent.swift');
    const destDir = path.join(cfg.modRequest.platformProjectRoot, projectName);
    fs.copyFileSync(src, path.join(destDir, 'DictarNotaIntent.swift'));
    const relPath = `${projectName}/DictarNotaIntent.swift`;
    if (!cfg.modResults.hasFile(relPath)) {
      IOSConfig.XcodeUtils.addBuildSourceFileToGroup({
        filepath: relPath,
        groupName: projectName,
        project: cfg.modResults,
      });
    }
    return cfg;
  });
};
```

En `app.json`, `plugins` suma `"./plugins/withAppIntents.js"` (al final de la lista).

- [ ] **Step 3: Rebuild y verificar**

```bash
npx expo prebuild -p ios --clean
npx expo run:ios --device
```

Verificar que el proyecto compila (si `addBuildSourceFileToGroup` falla por el nombre del group, abrir `ios/<name>.xcodeproj` y revisar cómo se llama el group raíz; ajustar `groupName`). Manual en el iPhone: app Atajos → debería listar "Dictar nota"; decir "Oye Siri, dictar nota en Lumi" → la app abre en el modo voz. Nota: los App Shortcuts pueden tardar unos minutos/reinicio de la app en registrarse tras instalar.

- [ ] **Step 4: Commit** — `git add ios-assets plugins app.json && git commit -m "feat: App Shortcut de Siri para dictar nota (App Intent)"`

---

## FASE 5 — Notas cifradas (Keychain + Face ID)

### Task 11: Wrapper de cuerpo seguro (`secureBody`)

**Files:**
- Create: `src/notes/secureBody.ts`
- Test: `src/notes/__tests__/secureBody.test.ts` (solo la parte pura: `utf8ByteLength` y validación de límite)

**Interfaces:**
- Consumes: `expo-secure-store`.
- Produces: `SECURE_BODY_LIMIT = 2000`, `utf8ByteLength(s: string): number`, `class SecureBodyError extends Error { kind: 'too-long' }`, `saveSecureBody(noteId, body): Promise<void>`, `readSecureBody(noteId): Promise<string | null>`, `deleteSecureBody(noteId): Promise<void>`.

- [ ] **Step 1: Test que falla**

```ts
// src/notes/__tests__/secureBody.test.ts
import { utf8ByteLength, SECURE_BODY_LIMIT } from '../secureBody';

describe('utf8ByteLength', () => {
  it('cuenta bytes UTF-8, no chars', () => {
    expect(utf8ByteLength('abc')).toBe(3);
    expect(utf8ByteLength('ñ')).toBe(2);
    expect(utf8ByteLength('€')).toBe(3);
    expect(utf8ByteLength('🔒')).toBe(4);
  });
  it('el límite es 2000 bytes', () => {
    expect(SECURE_BODY_LIMIT).toBe(2000);
  });
});
```

Nota: este test importa el módulo que importa `expo-secure-store`; si Jest no lo resuelve, agregar stub en la config de Jest (`moduleNameMapper`: `'^expo-secure-store$': '<rootDir>/tests/mocks/expo-secure-store.ts'` con `export async function setItemAsync() {}` etc.).

Run: `npx jest src/notes/__tests__/secureBody.test.ts` → FAIL.

- [ ] **Step 2: Implementar**

```ts
// src/notes/secureBody.ts
import * as SecureStore from 'expo-secure-store';

// Límite práctico del Keychain para un item (iOS tolera más pero está desaconsejado).
export const SECURE_BODY_LIMIT = 2000;

export class SecureBodyError extends Error {
  kind: 'too-long';
  constructor(kind: 'too-long') {
    super(kind);
    this.kind = kind;
    this.name = 'SecureBodyError';
  }
}

export function utf8ByteLength(s: string): number {
  let bytes = 0;
  for (const ch of s) {
    const code = ch.codePointAt(0)!;
    bytes += code <= 0x7f ? 1 : code <= 0x7ff ? 2 : code <= 0xffff ? 3 : 4;
  }
  return bytes;
}

const keyFor = (noteId: string) => `note-body-${noteId}`;

export async function saveSecureBody(noteId: string, body: string): Promise<void> {
  if (utf8ByteLength(body) > SECURE_BODY_LIMIT) throw new SecureBodyError('too-long');
  await SecureStore.setItemAsync(keyFor(noteId), body, {
    requireAuthentication: true,
    authenticationPrompt: 'Desbloqueá para guardar la nota cifrada',
  });
}

export async function readSecureBody(noteId: string): Promise<string | null> {
  return SecureStore.getItemAsync(keyFor(noteId), {
    requireAuthentication: true,
    authenticationPrompt: 'Desbloqueá para ver la nota',
  });
}

export async function deleteSecureBody(noteId: string): Promise<void> {
  try {
    await SecureStore.deleteItemAsync(keyFor(noteId));
  } catch {
    /* no existía: ok */
  }
}
```

(Verificar opciones exactas en https://docs.expo.dev/versions/v57.0.0/sdk/securestore/ — `requireAuthentication` y `authenticationPrompt`.)

En `app.json`, cambiar el plugin `"expo-secure-store"` por:

```json
      [
        "expo-secure-store",
        { "faceIDPermission": "Lumi usa Face ID para desbloquear tus notas cifradas." }
      ]
```

- [ ] **Step 3: Verificar** — `npx jest src/notes && npx tsc --noEmit` → PASS/limpio.
- [ ] **Step 4: Commit** — `git add src/notes tests app.json && git commit -m "feat: almacenamiento de cuerpo cifrado en Keychain con Face ID"`

### Task 12: Columna `secure` + búsqueda que no expone cuerpos

**Files:**
- Modify: `src/db/schema.ts` (migración v3), `src/notes/types.ts`, `src/db/notesRepo.ts`
- Test: `src/db/__tests__/notesRepo.test.ts` (ampliar)

**Interfaces:**
- Produces: `Note.secure: boolean`; `createNote(db, { title, body, tag?, secure? })`; `updateNote` acepta `secure`; `listNotes` NO matchea por cuerpo en notas seguras.

- [ ] **Step 1: Tests que fallan**

```ts
  it('nota segura: guarda flag y la búsqueda no matchea su cuerpo', async () => {
    const d = db();
    await createNote(d, { title: 'Claves banco', body: '', secure: true }, { id: 's1' });
    await createNote(d, { title: 'Normal', body: 'clave wifi 1234', secure: false }, { id: 'n1' });
    expect((await getNote(d, 's1'))?.secure).toBe(true);
    // buscar por título de la segura funciona; por cuerpo (vacío en DB) no expone nada
    expect((await listNotes(d, 'banco')).map((n) => n.id)).toEqual(['s1']);
    expect((await listNotes(d, 'clave')).map((n) => n.id)).toEqual(expect.arrayContaining(['n1']));
    await updateNote(d, 'n1', { secure: true, body: '' });
    expect((await getNote(d, 'n1'))?.secure).toBe(true);
  });
```

Run: `npx jest src/db` → FAIL.

- [ ] **Step 2: Implementar**

- `schema.ts`: push a `MIGRATIONS`: `` `ALTER TABLE notes ADD COLUMN secure INTEGER NOT NULL DEFAULT 0;` `` (queda como tercer elemento, v3).
- `types.ts`: `secure: boolean;` en `Note`.
- `notesRepo.ts`: `Row.secure: number`; `toNote`: `secure: r.secure === 1`; `createNote` input suma `secure?: boolean`, INSERT agrega columna `secure` con `input.secure ? 1 : 0`; `updateNote` Pick suma `'secure'` y el UPDATE agrega `secure=?` con `next.secure ? 1 : 0`; en `listNotes` el WHERE de búsqueda pasa a:

```ts
    where.push('(lower(title) LIKE ? OR (secure = 0 AND lower(body) LIKE ?))');
```

- [ ] **Step 3: Verificar** — `npx jest && npx tsc --noEmit` → PASS/limpio.
- [ ] **Step 4: Commit** — `git add src/db src/notes && git commit -m "feat: flag secure en notas y búsqueda sin exponer cuerpos cifrados"`

### Task 13: UI de notas cifradas

**Files:**
- Modify: `src/app/note/[id].tsx` (toggle, carga con Face ID, guardado, cifrar/descifrar, borrar)
- Modify: `src/components/NoteCard.tsx` (badge 🔒, sin preview del cuerpo)

**Interfaces:**
- Consumes: `saveSecureBody`, `readSecureBody`, `deleteSecureBody`, `SecureBodyError` (Task 11); `Note.secure` (Task 12).

- [ ] **Step 1: `NoteCard.tsx`**

- Si `note.secure`: en lugar del cuerpo, mostrar `🔒 Nota cifrada` en `textMuted`; el título se muestra normal.

```tsx
      {note.secure ? (
        <Text style={[styles.body, { color: palette.textMuted }]}>🔒 Nota cifrada</Text>
      ) : (
        <Text numberOfLines={2} style={[styles.body, { color: palette.textMuted }]}>{note.body}</Text>
      )}
```

- [ ] **Step 2: `note/[id].tsx` — estados y carga**

- Estados nuevos: `const [secure, setSecure] = useState(false);` y `const [locked, setLocked] = useState(false);`
- En el load de nota existente:

```ts
      setSecure(n.secure);
      if (n.secure) {
        try {
          const body = await readSecureBody(n.id);
          if (body === null) { setLocked(true); } else { setBody(body); setLocked(false); }
        } catch (e) {
          log.warn('lectura de nota cifrada falló:', (e as Error).message);
          setLocked(true);
        }
      } else {
        setBody(n.body);
      }
```

(el `setBody(n.body)` actual se mueve a la rama else). Si `locked`, en lugar del TextInput del cuerpo mostrar:

```tsx
        <View style={{ alignItems: 'center', gap: 10, paddingVertical: 30 }}>
          <Text style={{ color: palette.textMuted }}>🔒 Contenido bloqueado</Text>
          <Pressable onPress={reloadSecureBody} style={[styles.saveBtn, { backgroundColor: palette.accent }]}>
            <Text style={styles.saveText}>Desbloquear</Text>
          </Pressable>
        </View>
```

donde `reloadSecureBody` reintenta `readSecureBody` con el mismo manejo.

- [ ] **Step 3: Toggle y guardado**

- Toggle (entre el TagChips y el ReminderPicker), oculto mientras `locked`:

```tsx
      <View style={{ flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between' }}>
        <Text style={{ color: palette.text, fontSize: 15 }}>🔒 Nota cifrada</Text>
        <Switch value={secure} onValueChange={setSecure} />
      </View>
```

(import `Switch` de react-native).
- `save()` pasa a:

```ts
    const finalTitle = title.trim() || 'Sin título';
    const targetId = isNew ? newId() : note!.id;
    try {
      if (secure) {
        await saveSecureBody(targetId, body); // Face ID + valida tamaño
      }
    } catch (e) {
      if (e instanceof SecureBodyError && e.kind === 'too-long') {
        Alert.alert('Nota muy larga', 'Las notas cifradas soportan hasta ~2000 caracteres. Acortá el contenido.');
        return;
      }
      Alert.alert('No se pudo cifrar', 'Verificá que el iPhone tenga código de bloqueo configurado.');
      return;
    }
    const storedBody = secure ? '' : body;
    let saved: Note;
    if (isNew) {
      saved = await createNote(db, { title: finalTitle, body: storedBody, tag, secure }, { id: targetId });
      if (pinned) saved = await updateNote(db, saved.id, { pinned: true });
    } else {
      saved = await updateNote(db, note!.id, { title: finalTitle, body: storedBody, pinned, tag, secure });
      if (!secure && note!.secure) await deleteSecureBody(note!.id); // descifrada: limpiar Keychain
    }
```

(el resto de `save()` — recordatorio y `router.back()` — queda igual; en `createNote` ya pasamos `id: targetId`).
- En `confirmDelete`, antes de `deleteNote`: `if (note!.secure) await deleteSecureBody(note!.id);`
- Ocultar "Editar con voz" también si `secure === true` (`{!isNew && hasApiKey && !secure && (…)}`) — las notas cifradas nunca pasan por la IA.

- [ ] **Step 4: Rebuild y verificar**

El `faceIDPermission` del plugin requiere prebuild:

```bash
npx expo prebuild -p ios
npx expo run:ios --device
```

`npx tsc --noEmit && npx jest && npx eslint src --max-warnings=10` → limpio.
Manual: crear nota cifrada con una key de prueba → guardar (pide Face ID) → en el listado se ve 🔒 sin preview → abrirla pide Face ID → cancelar Face ID muestra "Contenido bloqueado" + Desbloquear → descifrarla (toggle off + guardar) vuelve el cuerpo a la DB → borrarla no deja restos (guardar otra con el mismo flujo funciona). Probar nota >2000 bytes → alerta.

- [ ] **Step 5: Commit** — `git add src && git commit -m "feat: notas cifradas con Keychain y Face ID"`

---

### Task 14: Verificación final integral

- [ ] **Step 1: Suite completa** — `npx jest && npx tsc --noEmit && npx eslint src` (solo el error preexistente de `useDictation.ts`).
- [ ] **Step 2: Regresión manual en iPhone** — dictado normal (fase previa intacta), recordatorio con acciones, filtro por tags, share desde Safari, "Oye Siri, dictar nota en Lumi", nota cifrada end-to-end, todo en dark y light.
- [ ] **Step 3: Commit final de ajustes** si la regresión encontró detalles.
